node_modules
.next
out
build
.DS_Store
*.pem
npm-debug.log*
# All env files. .env*.local alone did NOT cover a plain .env, which is where
# the service-role key would sit — one `git add -A` from being in history.
.env
.env.*
!.env.example
.vercel
*.tsbuildinfo
next-env.d.ts
